import React from "react";
import "../App.css";

const Button = () => {
  //({ onClick }) => {
  return (
    <button className="lcars-element button">
      {/* onClick={onClick}> */}
      Click me!
    </button>
  );
};

export default Button;

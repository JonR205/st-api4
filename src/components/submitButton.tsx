import React from "react";

const Button = () => {
  //({ onClick }) => {
  return (
    <button className="lcars-navy-blue-bg.lcars-navy-blue-bg">
      {/* onClick={onClick}> */}
      Click me!
    </button>
  );
};

export default Button;
